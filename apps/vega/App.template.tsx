import React from 'react'
import { Root } from '@lingo/shared-ui'
// Vega entry. Sizes are px at 1080p → scale 1.
export default function App() { return <Root apiBaseUrl={'https://api.lingo.example'} scale={1} /> }
